# Generated by ChatGPT
[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "insta-tables"
version = "0.1.0"
description = "Extract tables from Instagram JSON exports for student projects"
readme = "README.md"
requires-python = ">=3.9"
dependencies = ["pandas>=1.5"]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
